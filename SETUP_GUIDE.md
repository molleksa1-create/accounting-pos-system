# 📖 دليل التثبيت والاستخدام

نظام المحاسبة والمخزون ونقطة البيع - نظام متكامل مفتوح المصدر

---

## 🚀 التثبيت السريع

### على Windows

1. **قم بتحميل المشروع من GitHub:**
   ```bash
   git clone https://github.com/molleksa1-create/accounting-pos-system.git
   cd accounting-pos-system
   ```

2. **انقر على ملف `setup.bat` مرتين:**
   - سيقوم البرنامج تلقائياً بـ:
     - ✅ تثبيت جميع المكتبات
     - ✅ تشغيل قاعدة البيانات
     - ✅ فتح المتصفح على `http://localhost:8000`
     - ✅ تشغيل السيرفر

3. **انتظر حتى يفتح المتصفح تلقائياً** 🎉

---

### على Linux/Mac

1. **قم بتحميل المشروع من GitHub:**
   ```bash
   git clone https://github.com/molleksa1-create/accounting-pos-system.git
   cd accounting-pos-system
   ```

2. **اجعل ملف `setup.sh` قابلاً للتنفيذ:**
   ```bash
   chmod +x setup.sh
   ```

3. **قم بتشغيل البرنامج:**
   ```bash
   ./setup.sh
   ```

4. **انتظر حتى يفتح المتصفح تلقائياً** 🎉

---

## 📝 الاستخدام اليدوي

إذا أردت التثبيت يدوياً:

### الخطوة 1: إنشاء بيئة افتراضية

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### الخطوة 2: تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### الخطوة 3: تطبيق الهجرات

```bash
python manage.py migrate
```

### الخطوة 4: تشغيل السيرفر

```bash
python manage.py runserver
```

### الخطوة 5: فتح المتصفح

افتح المتصفح واكتب:
```
http://localhost:8000
```

---

## 🎯 الميزات الرئيسية

✅ **إدارة المخزون** - تتبع المنتجات والمخزون  
✅ **نقطة البيع (POS)** - إنشاء فواتير مبيعات  
✅ **الفواتير** - إدارة فواتير الشراء والبيع  
✅ **التصنيع** - إدارة أوامر الإنتاج والوصفات  
✅ **التقارير** - تقارير مفصلة عن المبيعات والمخزون  
✅ **واجهة برمجية (API)** - للتطبيقات الخارجية  
✅ **دعم اللغة العربية** - واجهة كاملة بالعربية  

---

## 🔐 الوصول إلى لوحة التحكم

بعد فتح البرنامج، يمكنك الوصول إلى:

- **الصفحة الرئيسية:** `http://localhost:8000`
- **لوحة التحكم:** `http://localhost:8000/admin/`
- **واجهة برمجية:** `http://localhost:8000/api/v1/`

---

## 📱 إضافة بيانات

### إضافة شركة وفرع:

1. اذهب إلى `http://localhost:8000/admin/`
2. انقر على **Core** → **Companies**
3. أضف شركة جديدة
4. أضف فرع للشركة

بعد إضافة شركة وفرع، ستظهر الإحصائيات على الصفحة الرئيسية تلقائياً!

---

## 🛠️ استكشاف الأخطاء

### المشكلة: "Python غير مثبت"
**الحل:** قم بتثبيت Python 3.11 من https://www.python.org

### المشكلة: "الملف setup.bat لا يعمل"
**الحل:** اضغط على الملف بزر الفأرة الأيمن → اختر "Run as administrator"

### المشكلة: "خطأ في قاعدة البيانات"
**الحل:** احذف ملف `db.sqlite3` وشغل البرنامج مرة أخرى

### المشكلة: "المتصفح لم يفتح تلقائياً"
**الحل:** افتح المتصفح يدوياً واكتب `http://localhost:8000`

---

## 📚 الموارد الإضافية

- **GitHub:** https://github.com/molleksa1-create/accounting-pos-system
- **Django:** https://www.djangoproject.com/
- **Django REST Framework:** https://www.django-rest-framework.org/

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل، يرجى:
1. تحقق من أن Python مثبت بشكل صحيح
2. تأكد من أن جميع المكتبات مثبتة
3. حاول حذف مجلد `venv` وتشغيل البرنامج مرة أخرى

---

**استمتع باستخدام البرنامج!** 🎉✨

