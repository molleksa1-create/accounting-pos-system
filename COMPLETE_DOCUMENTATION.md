# نظام المحاسبة والمخزون ونقطة البيع المتكامل

## 📋 نظرة عامة

نظام محاسبي متكامل مفتوح المصدر يجمع بين:
- ✅ إدارة المحاسبة والفواتير
- ✅ إدارة المخزون والمنتجات
- ✅ نقطة البيع (POS)
- ✅ نظام التصنيع والوصفات
- ✅ التقارير والتحليلات
- ✅ تطبيق أندرويد للكاشير
- ✅ التكامل مع منصات التوصيل

---

## 🏗️ البنية المعمارية

### Backend (Django)
```
accounting_pos_system/
├── core/              # الأساسيات (المستخدمين، الشركات، الفروع)
├── inventory/         # إدارة المخزون والمنتجات
├── manufacturing/     # نظام التصنيع والوصفات
├── accounting/        # المحاسبة والفواتير
├── pos/              # نقطة البيع
├── reports/          # التقارير والتحليلات
├── web/              # واجهات الويب
├── api/              # REST APIs
├── delivery/         # التكامل مع منصات التوصيل
└── config/           # إعدادات المشروع
```

### Frontend (Web)
- لوحة تحكم (Dashboard)
- إدارة المنتجات
- إدارة الفواتير
- التقارير

### Mobile (Android)
- تطبيق الكاشير (POS)
- قارئ الباركود
- المزامنة الأوفلاين
- التقارير

---

## 🚀 البدء السريع

### 1. التثبيت

```bash
# استنساخ المشروع
git clone https://github.com/molleksa1-create/accounting-pos-system.git
cd accounting-pos-system

# إنشاء بيئة افتراضية
python -m venv venv
source venv/bin/activate  # على Windows: venv\Scripts\activate

# تثبيت المكتبات
pip install -r requirements.txt
```

### 2. إعداد قاعدة البيانات

```bash
# تطبيق الهجرات
python manage.py migrate

# إنشاء مستخدم إداري
python manage.py createsuperuser

# إضافة بيانات تجريبية (اختياري)
python manage.py shell < populate_data.py
```

### 3. تشغيل الخادم

```bash
python manage.py runserver
```

الآن يمكنك الوصول إلى:
- **الويب**: http://localhost:8000
- **لوحة التحكم**: http://localhost:8000/admin
- **APIs**: http://localhost:8000/api/v1/

---

## 📊 النماذج الرئيسية

### Core (الأساسيات)

| النموذج | الوصف |
|--------|-------|
| **Company** | الشركات |
| **Branch** | الفروع |
| **CustomUser** | المستخدمون |
| **Customer** | العملاء |
| **Supplier** | الموردون |
| **Category** | فئات المنتجات |
| **Unit** | وحدات القياس |

### Inventory (المخزون)

| النموذج | الوصف |
|--------|-------|
| **Product** | المنتجات |
| **InventoryMovement** | حركات المخزون |
| **StockAdjustment** | تعديلات المخزون |

### Manufacturing (التصنيع)

| النموذج | الوصف |
|--------|-------|
| **Recipe** | الوصفات |
| **RecipeIngredient** | مكونات الوصفة |
| **ProductionOrder** | أوامر الإنتاج |

### Accounting (المحاسبة)

| النموذج | الوصف |
|--------|-------|
| **PurchaseOrder** | أوامر الشراء |
| **PurchaseInvoice** | فواتير الشراء |
| **PurchaseInvoiceItem** | عناصر فاتورة الشراء |
| **GoodsReceipt** | إيصالات الاستقبال |

### POS (نقطة البيع)

| النموذج | الوصف |
|--------|-------|
| **SalesOrder** | أوامر البيع |
| **SalesInvoice** | فواتير البيع |
| **SalesInvoiceItem** | عناصر فاتورة البيع |
| **POSSession** | جلسات الكاشير |
| **POSTransaction** | معاملات نقطة البيع |

### Reports (التقارير)

| النموذج | الوصف |
|--------|-------|
| **Report** | التقارير العامة |
| **SalesReport** | تقارير المبيعات |
| **InventoryReport** | تقارير المخزون |
| **FinancialReport** | التقارير المالية |
| **DashboardMetric** | مقاييس لوحة التحكم |

### Delivery (التوصيل)

| النموذج | الوصف |
|--------|-------|
| **DeliveryPlatform** | منصات التوصيل |
| **DeliveryOrder** | طلبات التوصيل |
| **DeliveryTracking** | تتبع التوصيل |
| **DeliveryIntegrationLog** | سجلات التكامل |

---

## 🔌 REST APIs

### المنتجات

```
GET    /api/v1/products/              # قائمة المنتجات
GET    /api/v1/products/{id}/         # تفاصيل المنتج
GET    /api/v1/products/low_stock/    # المنتجات منخفضة المخزون
GET    /api/v1/products/by_barcode/   # البحث بـ barcode
```

### فواتير المبيعات

```
GET    /api/v1/sales-invoices/        # قائمة الفواتير
POST   /api/v1/sales-invoices/        # إنشاء فاتورة
GET    /api/v1/sales-invoices/today/  # فواتير اليوم
GET    /api/v1/sales-invoices/statistics/ # الإحصائيات
```

### العملاء

```
GET    /api/v1/customers/             # قائمة العملاء
GET    /api/v1/customers/{id}/        # تفاصيل العميل
```

### الموردين

```
GET    /api/v1/suppliers/             # قائمة الموردين
GET    /api/v1/suppliers/{id}/        # تفاصيل المورد
```

### معاملات نقطة البيع

```
GET    /api/v1/pos-transactions/      # قائمة المعاملات
POST   /api/v1/pos-transactions/      # إنشاء معاملة
```

### الوصفات

```
GET    /api/v1/recipes/               # قائمة الوصفات
GET    /api/v1/recipes/{id}/          # تفاصيل الوصفة
```

### أوامر الإنتاج

```
GET    /api/v1/production-orders/     # قائمة الأوامر
POST   /api/v1/production-orders/     # إنشاء أمر
PUT    /api/v1/production-orders/{id}/ # تحديث أمر
```

---

## 📱 تطبيق الأندرويد

### المميزات

✅ **واجهة الكاشير**
- عرض المنتجات بشكل جميل
- إضافة المنتجات للسلة
- حساب الإجمالي والضريبة
- طرق دفع متعددة

✅ **قارئ الباركود**
- قراءة باركود المنتجات
- إضافة سريعة للسلة
- البحث عن المنتجات

✅ **المزامنة**
- تحميل المنتجات من الخادم
- حفظ الفواتير محلياً
- إرسال الفواتير عند الاتصال

✅ **العمل الأوفلاين**
- قاعدة بيانات محلية (Room)
- المزامنة التلقائية
- إشعارات الحالة

✅ **التقارير**
- تقارير المبيعات اليومية
- إحصائيات الأداء
- تقارير المخزون

### التثبيت

```bash
cd android_app
./gradlew build
./gradlew installDebug
```

---

## 🚚 التكامل مع منصات التوصيل

### المنصات المدعومة

✅ **Hanger Station**
✅ **Kita**
✅ **Uber Eats** (قريباً)
✅ **Zomato** (قريباً)

### الميزات

- ✅ إنشاء طلبات توصيل تلقائية
- ✅ تحديث حالة الطلب
- ✅ تتبع الطلب في الوقت الفعلي
- ✅ إلغاء الطلبات
- ✅ سجل التكامل الكامل

### الاستخدام

```python
from delivery.models import DeliveryPlatform, DeliveryOrder
from delivery.services import DeliveryPlatformService

# الحصول على منصة التوصيل
platform = DeliveryPlatform.objects.get(platform_name='hanger')

# إنشاء خدمة التكامل
service = DeliveryPlatformService(platform)

# إنشاء طلب توصيل
result = service.create_order(delivery_order)

# تحديث حالة الطلب
service.update_order_status(delivery_order, 'on_the_way')

# إلغاء الطلب
service.cancel_order(delivery_order)
```

---

## 📊 التقارير

### التقارير المتاحة

1. **تقارير المبيعات**
   - المبيعات اليومية
   - المبيعات الشهرية
   - المبيعات السنوية
   - أفضل المنتجات مبيعاً

2. **تقارير المخزون**
   - المخزون الحالي
   - المنتجات منخفضة المخزون
   - حركات المخزون
   - تقييم المخزون

3. **التقارير المالية**
   - الإيرادات
   - المصروفات
   - الأرباح
   - الرصيد

4. **تقارير الموردين والعملاء**
   - أفضل الموردين
   - أفضل العملاء
   - الأرصدة المستحقة

---

## 🔒 الأمان

### الميزات الأمنية

✅ **المصادقة والتفويض**
- نظام مستخدمين متقدم
- أدوار وصلاحيات
- تشفير كلمات المرور

✅ **حماية البيانات**
- HTTPS للاتصالات
- تشفير البيانات الحساسة
- سجل تدقيق شامل

✅ **حماية API**
- Token-based authentication
- Rate limiting
- CORS protection

---

## 📝 التوثيق الإضافية

### ملفات التوثيق

- `README.md` - دليل البدء السريع
- `ARCHITECTURE.md` - البنية المعمارية
- `PROJECT_SUMMARY.md` - ملخص المشروع
- `COMPLETE_DOCUMENTATION.md` - هذا الملف

### روابط مفيدة

- [Django Documentation](https://docs.djangoproject.com/)
- [Django REST Framework](https://www.django-rest-framework.org/)
- [Android Documentation](https://developer.android.com/)
- [Kotlin Documentation](https://kotlinlang.org/docs/)

---

## 🤝 المساهمة

نرحب بمساهماتك! يرجى:

1. Fork المشروع
2. إنشاء فرع جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

---

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - انظر ملف `LICENSE` للتفاصيل.

---

## 📞 الدعم

للمزيد من المساعدة:
- 📧 البريد الإلكتروني: support@example.com
- 💬 Telegram: @example
- 🐛 GitHub Issues: [Report Bug](https://github.com/molleksa1-create/accounting-pos-system/issues)

---

## 🎯 الخطوات التالية

- [ ] إضافة المزيد من منصات التوصيل
- [ ] تحسين واجهة الويب
- [ ] إضافة المزيد من التقارير
- [ ] تحسين الأداء
- [ ] إضافة الاختبارات الشاملة

---

**آخر تحديث**: 2025-10-25
**الإصدار**: 1.0.0
